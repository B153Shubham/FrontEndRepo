import { DashboardHeaderRole } from './dashboard-header-role';

describe('DashboardHeaderRole', () => {
  it('should create an instance', () => {
    expect(new DashboardHeaderRole()).toBeTruthy();
  });
});
